export interface ContactCreateModel {
  firstName: string;
  lastName: string;
  dateOfBirth: Date;
  phoneNumber: Text;
  iban: string;
}

export interface ContactModel {
  firstName: string;
  lastName: string;
  dateOfBirth: Date;
  phoneNumber: Text;
  iban: string;
}
